# Get a project ID at https://cloud.reown.com (WalletConnect Cloud)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Anthropic API key for the vault advisory agent (server-side only, never NEXT_PUBLIC_)
ANTHROPIC_API_KEY=

# No subgraph URL needed here anymore - @ixswap1/vault-agent-sdk ships the
# known vault's subgraph endpoint (and address) via its KNOWN_VAULTS registry.
